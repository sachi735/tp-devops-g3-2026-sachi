services:
  devops-tp-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: devops-tp-api
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      GIT_COMMIT: local
      BUILD_DATE: local
    restart: unless-stopped